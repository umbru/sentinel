# specify path to umbru.conf or leave blank
# default is the same as Umbru Core
#umbru_conf=/home/ryxor/.umbru/umbru.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
